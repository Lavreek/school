{% extends 'base.html.twig' %}
{% block body %}
    <form id="faculty-create" style="margin: auto;">
        <div class="mb-3">
            <label for="exampleInputEmail1" class="form-label">Название факультета</label>
            <input type="text" class="form-control" name="_title" id="title" aria-describedby="titleHelp">
            <input type="hidden" name="_user_id" value="{{ params['user'].getId() }}">
        </div>
        <div class="text-center">
            <button type="button" class="btn btn-primary faculty-button-create">Создать</button>
        </div>
        <div class="messages-collapse collapse" id="messages-collapse">
            <div class="display-messages py-4"></div>
        </div>
    </form>

{% endblock %}
